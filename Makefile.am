SUBDIRS = src po
EXTRA_DIST = config.rpath README NEWS COPYING

desktopfiledir=$(datadir)/applications
desktopfile_DATA=data/keyboard.desktop.in

EXTRA_DIST = config.rpath  keyboard.desktop.in

ACLOCAL_AMFLAGS = -I m4
